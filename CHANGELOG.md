# 更新日志

这里记录 VoxType 的重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。

## [未发布]

## [0.1.104] - 2026-06-08

### 修复

- 修复最终包 `result.text` 补齐尾字、但相对 `definite=true` 分句略短或轻微改写时，最终输出仍可能回退到缺尾字分句的问题；现在高度重合且长度未明显残缺的最终包会被优先保留。
- 停录尾音默认从约 `150ms` 调整为约 `250ms`，仍检测到人声时最多约 `400ms`；旧默认 `800ms`、`200ms`、`150ms` 和 `100ms` 会自动迁移到新默认。

## [0.1.103] - 2026-06-08

### 修复

- 修复最终包文本提取复用实时字幕策略的问题：实时字幕仍可优先显示更完整的 `utterances` 拼接文本，但最终粘贴优先使用豆包最终包里的 `result.text`，避免 `result.text` 已补齐尾字时被较长但缺尾字的分句文本覆盖。
- 停录尾音默认从约 `100ms` 调整为约 `150ms`，仍检测到人声时最多约 `300ms`，降低说完最后一个字立刻停止时的尾字截断概率；旧默认 `800ms`、`200ms` 和 `100ms` 会自动迁移到新默认。

### 工程治理

- 新增 ASR 质量与延迟守门清单，并把当前验证有效的速度/效果组合同步到代码注释、维护规则、README、Wiki 和音频片段审计，降低后续误回退风险。

## [0.1.102] - 2026-06-06

### 修复

- 修复豆包最终包已经补齐尾字、但前文相对 `definite=true` 稳定分段有轻微改写时，最终输出可能误用稳定分段而丢失最后一个字的问题；现在最终包与稳定分段高度重合且更长时，会优先使用最终包整段文本。

## [0.1.101] - 2026-06-06

### 改进

- 停录后尾音采集默认从约 `200ms` 进一步缩短到约 `100ms`；仍检测到人声时最多延长到约 `200ms`，降低点击结束后的体感等待。旧默认 `800ms` 和上一版默认 `200ms` 会自动迁移到新默认。
- 屏幕 OCR 上下文默认等待从 `300ms` 调整为 `500ms`，结合近期本机日志中 OCR 中位数约 `314ms`、P90 约 `437ms` 的表现，提高上下文命中率，同时继续超时跳过。
- 保持豆包首字返回加速默认开启，`accelerate_score = 8`，用于改善开头字幕出现速度。

## [0.1.100] - 2026-06-06

### 改进

- 停录后尾音采集默认从旧默认 `800ms` 收敛为约 `200ms`，仍检测到人声时最多延长到约 `400ms`，减少点击结束后的体感等待；旧默认配置会自动迁移到新默认。
- 首包头部保护不再缩短默认 `200ms` 音频包；约 `50ms` 头部静音仍并入第一包，但第一包总时长按当前分片配置发送，优先保障豆包识别稳定性。
- 悬浮字幕刷新提速改为本地链路优化：中间字幕节流从 `80ms` 降到 `50ms`，WebSocket 响应轮询从 `40ms` 降到 `20ms`，节流窗口内的新字幕会合并最新文本并按时补发，不改变豆包推荐的默认 `200ms` 音频分片。
- 实时字幕会在同一响应中优先采用更完整的累计分句文本，避免 `result.text` 暂时落后时字幕跟不上后续说话；收到豆包最终包后的额外 settle 从 `900ms` 收敛到 `300ms`，仍不使用中间结果作为最终粘贴文本。
- 默认开启豆包首字返回加速，`accelerate_score` 使用中等值 `8`，提升开头字幕出现速度；如首字准确率下降，可在配置中关闭或把加速率降到 `0`。
- 对齐豆包文档默认值，语义顺滑 `enable_ddc` 新默认关闭；已有配置里的显式取值会保留，避免覆盖用户选择。
- ASR 直传热词限制为前 `100` 个有效条目，手动热词优先于自动确认热词，避免热词过多导致实时请求变重。

## [0.1.99] - 2026-06-06

### 改进

- 新增麦克风输入增益 `audio.input_gain_db`，默认 `0 dB` 不改变现有行为；声音偏小但清晰时可在录音排障中先试 `+6 dB`，仍偏小时再试 `+12 dB`。
- 增益在客户端完成 16kHz、单声道、16-bit PCM 归一化后、发送豆包前应用，并做 16-bit 饱和限幅，避免整数溢出；本地音量条和静音自动停止也同步使用增益后的有效音量。
- 同步三语言设置文案、空识别排障提示、配置模板、README 和 Wiki，明确豆包文档没有单独的服务端增益参数，低音量清晰麦克风应由客户端抬升 PCM 电平。

## [0.1.98] - 2026-06-06

### 改进

- 豆包 `bigmodel_async + enable_nonstream` 二遍识别默认不再发送 `audio.language`，旧默认 `zh-CN` 也按自动/服务默认处理，更适合中文、英文、方言和混合输入；如需尝试固定非默认语种仍可在 API 配置页手动选择。
- 麦克风输入流运行中异常不再只写日志；现在会立即失败本轮并阻止残缺音频结果进入润色、粘贴、统计，同时音频采集 fallback 优先使用系统默认输入格式以降低驱动异常和重采样压力。
- 约 `50ms` 头部静音现在并入第一包真实音频，第一包约 `150ms`，后续默认 `200ms`，保留开头保护的同时避免向豆包发送独立 50ms 小包。

## [0.1.97] - 2026-06-06

### 修复

- 停录后不再额外追加尾部静音；现有停录尾音等待和音频 flush 已覆盖最后发音，最后一个真实尾包会直接作为负包发送，减少结束后的体感等待。

## [0.1.96] - 2026-06-06

### 改进

- 优先把豆包返回的实时累计文本用于悬浮字幕，并将字幕刷新限流从 220ms 降到 80ms，让中间反馈更快显示。
- 正常真实音频分片和发包间隔保持在豆包建议的 100-200ms 范围内；首尾辅助静音只各补 50ms，最终输出仍只使用豆包最终包。
- ASR 结束发完负包后使用正常响应等待节奏，不再沿用发包节流造成 1ms 轮询。
- 屏幕 OCR 默认等待从 700ms 降到 300ms；超时作为正常跳过处理，减少 ASR 建连前的人为等待。

## [0.1.95] - 2026-06-06

### 修复

- 修复录音进度框在麦克风启动阶段没有即时反馈的问题：触发后先显示启动状态，麦克风就绪后再切换为录音态。
- 修复 ASR 发送节流阻塞 WebSocket 响应读取的问题，避免实时字幕和最终状态被本地发包等待拖慢。
- 修复 100ms 头尾补静音仍按 200ms 节奏发送的问题，避免人为制造停顿影响豆包头尾识别。
- 修复 `segment_ms` 被改成过小或过大时，实际 ASR 分片可能偏离豆包 100-200ms 建议范围的问题。
- 修复停止录音时额外发送空负包的问题；现在最后一个带音频的包直接作为负包发送，更贴合豆包协议语义。

### 改进

- ASR 实际发送分片统一限制在 100-200ms，默认保留豆包双向流式推荐的 200ms。
- 发包间隔按每个音频包实际 PCM 时长计算，不足一片的尾包和 100ms 补片不会被默认分片节奏拉长。
- 保留只使用豆包最终包的策略；中间结果仍只用于字幕反馈，不进入润色、粘贴、最近上下文、自动热词历史或成功统计。
- 同步 README、英文 README、配置模板、Wiki 草稿和 ASR 音频发送审计说明。

### 审计

- 本次改动影响 ASR WebSocket 音频发送队列、发包节奏、负包发送方式、麦克风分片边界、录音启动状态提示、配置模板、README、英文 README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 空识别仍进入失败态；不改 LLM 润色触发条件、剪贴板写入/恢复实现、热键默认值、托盘行为、统计正文记录、日志脱敏策略或配置字段结构。

## [0.1.94] - 2026-06-06

### 修复

- 修复豆包 ASR `single` 增量结果在部分场景下可能缺少开头分句的问题，主链路改为强制使用 `full` 全量最终包，并继续禁止使用中间结果作为最终文本。
- 修复最终包补齐逻辑只支持补尾、不支持补开头的问题；当最终包覆盖二遍分句且更完整时，可用最终包补齐开头或尾部。
- 修复停录时重采样器内部不足一个输出采样窗口的残留样本可能未进入 ASR 队列的问题。
- 修复非完整声道帧可能被 `chunks_exact` 静默丢弃的问题。

### 改进

- 在首个真实音频包前发送短头部静音，帮助豆包稳定识别开头几个字。
- ASR 音频包、头部静音、尾部静音按 100-200ms 节奏发送，避免连接启动或上下文等待造成积压后瞬间打包影响识别。
- 麦克风采集真正启动后才显示录音悬浮字幕，减少用户在采集未就绪时提前开口导致的开头缺失。
- 停录后继续 flush 重采样残留和未满分片尾包，发送尾部静音、负包，并等待豆包最终包及短 settle。
- 补充头部静音、发送节奏、补开头最终包、重采样残留、未满分片尾包和非完整声道帧测试。

### 审计

- 本次改动影响 ASR 请求组装、ASR WebSocket 音频发送节奏与最终结果选择、麦克风 PCM flush、录音启动状态提示、配置模板、README、英文 README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 空识别仍进入失败态；不改 LLM 润色触发条件、剪贴板写入/恢复实现、热键默认值、托盘行为、统计正文记录、日志脱敏策略或配置字段结构。

## [0.1.93] - 2026-06-06

### 修复

- 修复点击结束后尾音采集过早停止的问题：停录后保留内部最短采集窗口，仍检测到人声时继续等到安静或达到上限。
- 修复不足一个音频分片的尾部 PCM 可能未发送给豆包的问题，关闭麦克风前会 flush 尾部分片。
- 修复最终包整段文本比 `definite=true` 分句更完整时仍输出缺尾分句的问题，并在补尾判断中忽略常见标点和空白差异。

### 改进

- 发送豆包结束包前按正常分片追加短静音，帮助触发二遍最终判停；收到最终包后增加短 settle 窗口，等待后续二遍修正。
- 多声道转单声道时避免左右声道相位抵消导致人声被平均成近似静音。
- 补充停录尾音、最终包 settle、最终包超时、多声道相位抵消和静音补尾分片边界测试。

### 审计

- 本次改动影响停录状态机、音频采集分片、ASR WebSocket 结束包和最终结果选择、README、英文 README、配置模板、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 空识别仍进入失败态；不改 LLM 润色触发条件、剪贴板写入/恢复实现、热键默认值、托盘行为、统计正文记录、日志脱敏策略或配置字段结构。

## [0.1.92] - 2026-06-06

### 修复

- 停录后必须等到豆包最终包才允许输出，连接提前结束或未返回完整最终结果时进入失败态，不再粘贴可能缺尾的中间文本。
- 主链路强制使用豆包二遍最终结果所需参数：开启二遍识别、开启 utterances、使用 `single` 增量返回，并关闭加速文本，避免配置误改导致使用低可信中间结果。
- 麦克风采集结果发送豆包前统一转换为 `16000Hz`、单声道、16-bit PCM，避免 Windows 设备实际输出 48k/双声道时影响识别质量。

### 改进

- 16k 音频规范化新增简单窗口平均下采样，减少 48k/44.1k 转 16k 时因取点导致的语音能量丢失。
- 补充 ASR 最终包、二遍分句、音频格式转换、跨回调重采样和上采样边界测试。
- 同步 README、英文 README、配置模板和 Wiki 草稿，明确实时字幕只作反馈，最终输出只使用完整二遍最终结果。

### 审计

- 本次改动影响 ASR 请求组装、ASR WebSocket 最终结果选择、麦克风 PCM 规范化、配置样例、README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 空识别仍进入失败态；不改 LLM 润色触发条件、剪贴板写入/恢复实现、热键默认值、托盘行为、统计正文记录、日志脱敏策略或配置字段结构。

## [0.1.91] - 2026-06-06

### 修复

- 对齐豆包 `bigmodel_async` 二遍识别语义：最终进入润色、剪贴板输出、最近上下文、自动热词历史和统计的文本只使用 `definite=true` 的二遍分句，不再用实时中间结果兜底。
- 修复同一时间段二遍分句后续被服务端修正时，本地仍保留早期文本的问题；现在会用最新 `definite=true` 分句覆盖旧文本。

### 改进

- 将 ASR `result_type` 默认值从 `full` 调整为 `single`，减少重复中间结果；旧默认 `full` 会在配置加载时自动迁移为 `single`。
- 同步配置模板、前端兜底默认值、README 和 Wiki 草稿，明确实时字幕仅用于反馈，最终粘贴只使用豆包二遍最终文本。

### 审计

- 本次改动影响 ASR WebSocket 结果选择、ASR 请求默认参数、配置默认值迁移、配置样例、前端默认值、README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 空识别仍进入失败态；不改 LLM 润色触发条件、剪贴板写入/恢复实现、热键默认值、托盘行为、统计正文记录、日志脱敏策略或配置字段结构。

## [0.1.90] - 2026-06-04

### 修复

- 修复悬浮字幕透明度设置可能不透出桌面背景的问题：创建字幕 WebView 时显式设置透明窗口/WebView 背景，避免半透明字幕叠在不透明底色上。
- 为悬浮字幕和启动提示页补充明确的 `data-voxtype-mode` 页面模式标记，减少依赖 `:has()` 选择器导致的透明背景兼容风险。

### 审计

- 本次改动影响悬浮字幕窗口背景、前端 overlay/toast 模式样式、版本元数据、更新日志和发布审计记录。
- 不改 ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、统计正文记录、日志脱敏策略或配置结构。

## [0.1.89] - 2026-06-03

### 改进

- 将停止录音后的尾音保留默认值从 500ms 调整为 800ms，减少松开热键或停止录音时末尾字被截断的概率。
- 同步配置模板、前端兜底默认值、README 和 Wiki 草稿中的录音配置示例。

### 审计

- 本次改动影响音频默认配置、配置样例、前端默认值、README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 不改 ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.88] - 2026-05-31

### 改进

- 默认大模型提示词改为更偏“成稿化”的长文本润色策略：短消息仍轻量纠错，长段口述、记录、复盘、说明、会议纪要、产品反馈和投资复盘默认整理为更正式的自然段正文。
- 默认 User Prompt Template 同步强调保留原文事实、判断、语气强弱和立场，同时允许长文本适当调整句序、去掉口述感、重复和改口。

### 审计

- 本次改动影响默认 LLM 提示词、配置样例、前端默认值、README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 不改 LLM 模型参数、Base URL、`thinking_strategy`、全局 `max_tokens`、ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.87] - 2026-05-30

### 改进

- 发布默认大模型提示词的长度感知版本：短文本保持轻量纠错，长文本可删除口水词、重复和自我修正过程，并在不新增事实、不改变意图的前提下整理结构。
- 默认 User Prompt Template 增加紧凑提醒，强化“长文本去冗余、去改口、不主动添加 Markdown 或反引号”的当前请求行为。
- 同步打包已推荐的默认 LLM 配置、配置模板、前端默认值、README、英文 README、Wiki 草稿和 prompt 测试审计记录。

### 审计

- 本次改动影响默认 LLM 提示词、配置样例、前端默认值、README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 不改 LLM 模型参数、Base URL、`thinking_strategy`、全局 `max_tokens`、ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.86] - 2026-05-30

### 改进

- 默认大模型 System Prompt 增加“输入结构与优先级”和“技术文本规则”，明确待润色文本是唯一输出来源，参考信息只用于纠错。
- 默认 User Prompt Template 增加 `[待润色文本开始]` / `[待润色文本结束]` 边界，降低屏幕 OCR、最近上下文、用户场景和热词被误当作正文的概率。
- LLM 润色请求拼装进一步收紧参考信息规则：最近上下文不得被续写、复述或总结；屏幕 OCR 仅在与待润色文本相关时用于纠错；文件路径、命令、日志字段和代码标识符不确定时保留原样。
- 同步更新配置模板、前端默认值、README、英文 README、Wiki 草稿和 prompt 拼装审计记录。

### 审计

- 本次改动影响默认 LLM 提示词、LLM 润色参考信息拼装、配置样例、前端默认值、README、Wiki 草稿、版本元数据、更新日志和发布审计记录。
- 不改 LLM 模型参数、Base URL、`thinking_strategy`、全局 `max_tokens`、ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.85] - 2026-05-30

### 新增

- 大模型润色新增 `thinking_strategy` 思考适配配置，默认 `auto`；可按服务商选择 DashScope `enable_thinking=false`、DeepSeek/MiMo `thinking.type=disabled`、OpenRouter 低 reasoning effort 或不发送思考字段。
- API 配置页新增“思考适配”选择项；测试大模型连接时会尝试候选关闭策略，选择耗时最短的成功结果并保存到配置。

### 改进

- 实际润色和自动热词生成共用同一套 LLM 请求适配逻辑，避免不同链路对 thinking/reasoning 字段处理不一致。
- 非兼容服务商拒绝关闭思考字段时，会自动省略相关字段后重试，减少因未知参数导致的失败。
- 文档、配置模板、Wiki 和三语言文案同步说明思考适配策略，以及更换 Base URL 或模型后应重新测试。

### 审计

- 本次改动影响 LLM 请求参数、LLM 连接测试、自动热词生成请求参数、配置结构、API 配置页、三语言文案、配置样例、README、Wiki 草稿、版本元数据和发布审计记录。
- `npm run ai:release-check` 和 `npx tauri build` 均通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的上游 warnings。
- 不改默认大模型提示词，不新增全局 `max_tokens` 限制，不改 ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.84] - 2026-05-30

### 改进

- 精简默认大模型润色提示词和配置模板，保留“不聊天、不新增事实、金融数字谨慎处理”等核心约束，减少最终润色请求的 prompt 体积。
- 大模型润色和自动热词生成默认显式发送 `enable_thinking=false`；非阿里云兼容接口如果不支持该字段，会自动重试一次不带该字段的请求，兼顾速度和兼容性。
- 最终 LLM 润色请求不全局压低 `max_tokens`，避免 StepFun 等模型在第三方兼容接口下因输出预算过小返回空内容。
- 大模型 Base URL 支持根地址、`/v1` 和完整 `/v1/chat/completions` 等等价写法，润色测试、实际润色和自动热词生成共用同一规范化逻辑。
- 同步更新 README、英文 README、配置模板、Wiki 草稿和三语言设置文案，说明简化提示词、关闭思考模式和 Base URL 等价写法。

### 审计

- 本次改动影响默认 LLM 润色提示词、LLM 请求参数、自动热词生成请求参数、Base URL 规范化、配置样例、三语言文案、README、Wiki 草稿、版本元数据和发布审计记录。
- `npm run ai:release-check` 和 `npx tauri build` 均通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的上游 warnings。
- 不改 ASR 请求协议、空识别失败态、LLM 润色触发条件、屏幕 OCR 默认开关、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.83] - 2026-05-28

### 修复

- 选项页快捷跳转改为按当前 sticky 快捷导航的实际高度计算滚动位置，避免跳到“更新与诊断”等分组时标题被遮挡。

### 改进

- 统计分析页汇总卡片在常见桌面窗口宽度下尽量保持 4 张同排展示，窄窗口再切换为 2 列，减少不必要的纵向占用。
- 精简“节省时间”卡片说明文案为“按中文输入约每分钟50字估算”，并同步繁体中文和英文文案。

### 审计

- 本次改动只影响前端滚动定位、统计页响应式布局、三语言文案、版本元数据、更新日志和发布审计记录。
- 不改 ASR 请求协议、空识别失败态、LLM 润色启用条件、剪贴板输出、热键默认值、托盘行为、统计计算逻辑、统计正文记录或日志脱敏策略。

## [0.1.82] - 2026-05-28

### 改进

- 统计分析页将“按中文输入约每分钟50字估算节省时间”说明移动到“节省时间”卡片下，并优化汇总卡片在不同窗口宽度下的布局稳定性。
- 选项页新增顶部快捷跳转区，可直接定位到启动、麦克风、输入结果、屏幕文字、悬浮字幕和更新诊断设置。
- 切换左侧页面时自动回到新页面顶部，避免从长设置页切换后仍停留在旧滚动位置。
- 标题栏“最小化到托盘”按钮改用更贴近托盘收起语义的图标，降低与下载操作的误解。

### 审计

- 本次改动只影响前端展示、页面内导航、三语言文案、统计页展示测试、版本元数据和发布审计记录。
- 不改 ASR 请求协议、空识别失败态、LLM 润色启用条件、剪贴板输出、热键默认值、托盘行为、统计正文记录或日志脱敏策略。

## [0.1.81] - 2026-05-27

### 改进

- 收紧默认大模型提示词和参考信息拼接规则，明确用户词典、场景上下文、最近上下文和屏幕 OCR 不能把待润色文本没说的内容补进输出。
- 统一“润色时参考最近上下文”开关的排版和颜色，避免新增配置项在同组设置中过度突出。
- 将最终提示词预览从系统 alert 改为应用内弹窗，支持滚动查看和一键复制，并同步三语言预览文案与真实请求规则。

### 审计

- 本次改动影响默认大模型提示词、提示词预览弹窗、热词与提示词页开关样式、三语言文案、配置样例、README、Wiki 草稿、架构说明、版本元数据和审计记录。
- `npm run ai:release-check`、`npm audit --registry=https://registry.npmjs.org` 和 `npx tauri build` 均通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的上游 warnings。
- 不改 ASR 请求协议、空识别失败态、LLM 润色启用条件、剪贴板输出、热键默认值、托盘逻辑、统计正文记录或日志脱敏策略。

## [0.1.80] - 2026-05-27

### 新增

- 大模型润色新增可选“润色时参考最近上下文”，默认关闭；开启后仅在最近上下文本地保存也开启且润色实际触发时，将受限前文作为参考信息发送给大模型。

### 改进

- 热词与提示词页、隐私与本地数据页、提示词预览、配置模板、README 和 Wiki 草稿同步说明最近上下文进入大模型的隐私边界。

### 审计

- 本次改动影响 LLM 润色参考信息拼接、配置默认值、热词与提示词页、隐私与本地数据页、提示词预览、三语言文案、配置样例、README、Wiki 草稿、架构说明、版本元数据和发布审计记录。
- `cargo test`、`npm run check`、`npm run build`、`npm run scan:secrets` 和 `npm run ai:check` 已通过；发布前继续运行 `npm run ai:release-check`、`npm audit --registry=https://registry.npmjs.org` 和 `npx tauri build`。
- 不改 ASR 请求协议、空识别失败态、LLM 润色启用条件、剪贴板输出、热键默认值、托盘逻辑、统计正文记录或日志脱敏策略。

## [0.1.79] - 2026-05-25

### 改进

- 优化大模型润色提示词拼接，将用户词典、场景与产品偏好、屏幕 OCR 统一放入“参考信息”分区，并明确这些内容不是待润色文本或指令来源。
- 同步提示词预览的三语言文案，预览会展示参考信息使用规则和屏幕 OCR 占位说明，避免和真实请求拼接逻辑脱节。
- 默认大模型提示词补充“保持原文主要语言、不主动翻译中文或外语内容”的边界，降低外语听写和中英混合输入被意外翻译的概率。
- API 配置页新增启用润色后的数据发送说明，明确最终识别文本、用户词典、场景/产品偏好和屏幕 OCR 与大模型的关系。
- 提示词预览摘要改为区分最近上下文和屏幕 OCR 当前开关策略，避免关闭 OCR 后仍看到容易误解的占位说明。
- 热词与提示词页为最小润色字数补充范围、错误提示和说明；`llm_post_edit.min_chars` 增加 0 到 10000 的保存前校验。
- README、英文 README、Wiki 草稿、配置模板和架构说明同步补充 LLM 参考信息、原语言保留和参数边界。

### 审计

- 本次改动影响 LLM 润色提示词、前端提示词预览、API 配置页提示、三语言文案、配置校验、配置样例、文档说明、版本元数据和发布审计记录。
- `npm run ai:release-check`、`npm audit --registry=https://registry.npmjs.org` 和 `npx tauri build` 均通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的上游 warnings。
- 不改 ASR 请求协议、LLM 润色触发条件、屏幕 OCR 默认开关、剪贴板输出、热键默认值、托盘逻辑、统计正文记录或日志脱敏策略。

## [0.1.78] - 2026-05-23

### 改进

- 大模型 API 配置区默认直接展开，移除“展开配置/收起配置”按钮，减少首次填写 API 信息的操作步骤。
- 大模型 API 的 Base URL、API Key 和模型字段补充就地提示，降低把豆包 ASR Key 或不同地域模型信息填错的概率。
- 默认大模型提示词补充明显错词漏字、语法残缺和不通顺语句的修正规则，降低 ASR 偶发识别错误对最终文本的影响。

### 审计

- 本次改动只影响 API 配置页展示、三语言文案、默认大模型提示词、配置样例、README/Wiki 草稿、版本元数据和发布审计记录。
- 不改 ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘逻辑、统计正文记录或日志脱敏策略。

## [0.1.77] - 2026-05-23

### 改进

- API配置页新增豆包 ASR 官方帮助文档入口，方便首次配置时对照 App Key、Access Key 等字段说明。
- 大模型 API 测试改用示例文本和正式大模型提示词发起调用，成功时返回本次延迟。
- 热词与提示词页将“润色提示词”改名为“大模型提示词”，并移除编辑区收起按钮，默认直接显示提示词编辑框。
- API配置页将页面说明、健康检查和具体 API 表单分层展示；热词与提示词页前置场景上下文，并调整提示词说明，降低首次配置理解成本。
- 选项页移除“隐私与本地数据”跳转卡片，隐私数据入口保留在左侧导航。
- 选项页按常用设置、体验增强、应用维护重新梳理顺序，并合并麦克风/录音排障、更新/诊断区域，减少配置项割裂感。

### 审计

- 本次改动只影响配置页和提示词相关的前端展示、三语言文案、文档、LLM API 测试返回信息、版本号和发布元数据。
- 发布前根据 `npm audit` 结果刷新前端锁定依赖，升级 `@sveltejs/kit`、`svelte` 和 `devalue` 的实际安装版本以清理 npm 安全告警。
- 不改 ASR 请求协议、空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、托盘逻辑、统计正文记录或日志脱敏策略。

## [0.1.76] - 2026-05-22

### 改进

- 优化默认大模型润色提示词，按“核心边界、润色目标、数字与金融表达、句末处理”重组，提升语音输入后处理稳定性。
- 默认提示词新增金融、投资、量化语境下的数字化表达规则，例如 `100万`、`1%`、`10%`，同时明确不计算收益、不回答原文问题。
- README、英文 README、配置模板和 Wiki 草稿补充豆包 ASR 必填项、大模型可选项、测试入口和常见失败排查，降低首次配置卡点。

### 审计

- 本次改动影响 LLM 润色默认提示词、配置样例、文档、版本元数据和当前安装目录配置；不改 ASR 请求协议、LLM 润色触发条件、剪贴板输出、热键、托盘、统计正文记录或日志脱敏策略。

## [0.1.75] - 2026-05-10

### 新增

- 屏幕 OCR 上下文新增识别范围设置，默认识别当前显示器，并可在选项页切换为仅当前窗口。

### 改进

- 优化英文界面中 Prompts、recognition terms、Services、screen text context 等文案，使信息结构更贴近实际用途。
- 缩小统计分析页汇总卡片的默认尺寸和间距，减少默认窗口尺寸下的非必要换行。
- 在统计分析页补充隐私说明，强调该页只统计字数、语音时长和节省时间，不保存识别正文。
- README、英文 README 和 Wiki 草稿同步更新屏幕 OCR 默认范围、隐私边界、服务配置命名和候选生成历史说明。

### 审计

- 本次改动新增 `screen_context.capture_scope` 配置项，默认值为 `screen`，可选值为 `screen` 或 `window`。
- 本次改动不改变空识别失败态、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录、托盘逻辑或日志脱敏策略。

## [0.1.74] - 2026-05-09

### 改进

- 对主界面做一轮轻量 UI 精修，收敛字体层级、页面间距、侧栏选中态、卡片圆角和阴影强度。
- 优化首页语音状态卡片和输入表现卡片的配色与交互反馈，降低不必要的紫色渐变和高光。
- 优化统计分析页汇总卡片和按日统计表格的间距、边框和悬停状态，提升扫读体验。

### 审计

- 本次改动只影响前端样式和发布元数据；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.73] - 2026-05-09

### 修复

- 将 Windows/Tauri 图标集改为透明外底版本，避免托盘图标和深色任务栏菜单里出现白色外底。
- 补齐 NSIS 安装包和卸载器自身图标配置，确保安装包 exe、应用 exe、快捷方式和任务栏右键菜单都使用新版蓝白图标资源。

### 改进

- 精修标题栏和启动提示中的应用图标尺寸与容器样式，移除额外描边和阴影，让 SVG 图标在小尺寸下更清晰。

### 审计

- 本次改动只影响品牌图标资源、前端图标展示、Tauri 打包配置和发布元数据；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.72] - 2026-05-09

### 改进

- 替换 VoxType 应用图标为白蓝色麦克风按键图标，统一主界面标题栏、启动提示、favicon、托盘、快捷方式、窗口和安装包图标资源。
- 重新生成 Tauri Windows/macOS 图标集和托盘 RGBA 图标，减少旧蓝紫色图标在不同入口残留。

### 审计

- 本次改动只影响品牌图标资源、前端图标引用和发布元数据；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.71] - 2026-05-09

### 改进

- 优化英文界面文案，缩短容易挤出边界的状态说明、按钮和设置项，并统一 Services、Terms、Options、Privacy、Analytics 等页面术语。
- 抽取设置页通用样式，统一表单面板、分组标题、输入框、复选框、说明文字和操作按钮的尺寸与状态，减少同类元素在不同页面的样式漂移。
- 精修统计分析页标题对齐，使 Analytics 标题与统计卡片左边缘一致。
- 收敛隐私页和统计页残留的重复容器间距定义，让页面排版和卡片圆角更一致。

### 测试

- 使用浏览器自动化巡检 Home、Terms、Services、Options、Privacy、Analytics 页，覆盖 600px 与 2048px 宽度，没有发现主页面横向溢出。

### 审计

- 本次改动只影响前端样式、英文文案和发布元数据；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.70] - 2026-05-09

### 改进

- 优化首页、左侧导航和首页就绪卡片的视觉选中态，移除左侧竖向标记，让选中状态更简洁。
- 统一“隐私与本地数据”页保存位置的字体和样式，避免文件名与说明文字混用等宽字体造成视觉割裂。
- 收敛主要页面文案中的 ASR、LLM、OCR、Prompt、上传等工程化表达，改为更易理解的“语音识别”“大模型”“屏幕文字识别”“提示词”“发送”等用户语言。
- 优化统计分析页在窄窗口下的按日统计布局，避免指标列挤出内容区。

### 测试

- 使用浏览器自动化巡检首页、热词与提示词、API 配置、选项、隐私与本地数据和统计分析页，覆盖 2048px 与 600px 宽度，没有发现主页面横向溢出。

### 审计

- 本次改动只影响前端展示、三语言文案、响应式排版和发布元数据；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.69] - 2026-05-09

### 工程治理

- 优化 `npm run check:governance` 的 i18n key parity 检查，支持递归识别嵌套 `as const` / `satisfies` 文案对象，并移除脚本中的未使用字段。
- 补充治理脚本自测，覆盖真实 i18n 写法、缺失 key 和多余 key 场景，降低后续三语言文案漂移风险。

### 审计

- 本次改动只影响治理脚本和发布元数据；不改应用运行时代码、配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.68] - 2026-05-09

### 改进

- API 配置页在 ASR 密钥缺失时显示三步首次配置引导，帮助普通用户按“填 Key、测试连接、回首页开始输入”的顺序完成上手。
- 首次缺少 ASR 密钥时自动进入 API 配置页顶部，先展示引导与健康检查；点击“填写 ASR 密钥”后再定位到输入框。

### 工程治理

- `npm run check:governance` 增加三语言 i18n key parity 检查，覆盖嵌套文案对象，避免后续新增用户可见文案时漏同步简体中文、繁体中文或英文。
- `npm run test:governance` 补充 i18n 漏同步回归用例。

### 审计

- 本次改动只影响首次配置引导、前端导航定位、三语言文案、文档和治理脚本；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.67] - 2026-05-09

### 新增

- “隐私与本地数据”页补充配置文件与密钥、日志与诊断报告的保存位置、上传边界和脱敏口径。
- 标题栏新增自动保存状态提示，设置修改时会显示“更改将自动保存 / 正在保存设置 / 设置已保存”。

### 改进

- 优化“隐私与本地数据”页的分组、计数展示和清理按钮状态；自动热词历史显示条数与近似字数，清理按钮在状态未读取前保持不可点。
- 在隐私页开启 LLM 润色时，如果大模型 API 未配置，会引导到 API 配置而不是直接打开开关。
- 优化左侧导航的图标语义、活动态、焦点样式和短窗口滚动体验。
- README、英文 README 和 Wiki 草稿同步补充隐私与本地数据、自动保存状态提示和配置说明。

### 审计

- 本次改动只影响前端信息展示、设置反馈、文案和文档；不改配置结构、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认值、统计正文记录或日志脱敏策略。

## [0.1.66] - 2026-05-09

### 新增

- 新增“隐私与本地数据”页面，并在左侧导航和选项页提供入口；集中说明最近上下文、自动热词历史、统计数据、ASR 音频、屏幕 OCR、LLM 润色文本和剪贴板快照的保存与上传边界。
- 新增本地数据状态和统计清空命令，支持在界面中清空最近上下文、自动热词历史和使用统计。
- 新增 `docs/architecture.md`，说明 Svelte/Tauri/Rust 主链路、generation、剪贴板恢复、统计口径、隐私边界和 ASR/LLM/OCR 数据流。
- 托盘右键菜单新增“问题反馈 / Report issue”入口，打开 GitHub Issue 反馈页面。

### 改进

- 选项页将主快捷键与备用触发方式合并到“输入触发”分组，减少快捷键相关设置分散的问题。
- 托盘图标改为左键单击即可打开主窗口，同时保留双击打开兼容。
- README、英文 README 和 Wiki 草稿同步补充隐私与本地数据页面说明。

### 修复

- 清空使用统计时会删除所有候选统计文件，避免开发目录、安装目录或上级目录中残留旧 `voice_input_stats.jsonl` 后，刷新统计又读回旧记录。

### 测试

- 补充统计清空单元测试，覆盖多个候选统计文件和重复路径场景。

## [0.1.65] - 2026-05-09

### 文档

- 更新 README 和 Wiki 草稿的界面截图，改用 2026-05-09 的首页、API 配置和健康检查预览，并移除失效的旧首页截图引用。
- 补充英文 Wiki 配置指南仓库镜像，降低 README 英文链接与本地 Wiki 草稿漂移。
- 新增仓库内文档索引，补齐 Wiki Home、Sidebar、功能指南中英文草稿，并优化贡献、支持、安全和目录规范中的文档同步说明。

### 工程治理

- 新增 `npm run check:governance`，自动检查 `package.json`、`package-lock.json`、Tauri 和 Cargo 版本号一致性、Markdown 本地链接、截图引用和 README/Wiki 镜像覆盖情况，并接入 `npm run ai:check` 与 GitHub Actions CI。
- 新增 `npm run test:governance`，为治理检查脚本覆盖通过、版本不一致、锁文件版本不一致和 Wiki 镜像缺失等回归场景。
- 拆出前端通知和统计 controller，减少 `VoxTypeController.svelte.ts` 的横切状态职责，让主 controller 更接近组合入口。
- 将剪贴板输出中的 Win32 剪贴板和全局内存管理改为 RAII 小封装，降低后续修改时漏关剪贴板、漏解锁或错误释放内存的风险。

### 修复

- 统一最近 24 小时、最近 7 日和按日统计的节省时间口径，均按“手打等效时间 - 实际语音时长”展示，避免把 24 小时输入误显示为手打等效时间。

### 测试

- 新增 `npm run test:stats`，覆盖 24 小时、7 日和按日统计的节省时间净值口径，并接入 `npm run ai:check` 与 GitHub Actions CI。
- 补充剪贴板全局内存 ownership 转移单元测试，覆盖 `GlobalAlloc` 内存锁定后转交剪贴板所有权的关键路径。

## [0.1.64] - 2026-05-09

### 改进

- 优化首页空闲、处理中、成功和错误状态的视觉层级：顶部语音卡片更紧凑，成功结果提示弱化为通知式卡片，输入表现卡片的间距和图标样式更统一。
- 首页卡片按内容区全宽对齐，减少宽屏下不必要的居中留白，保持页面边缘更整齐。
- 刚启动且暂无最近结果时，首页中间层显示就绪提示卡，避免统计卡上移后底部出现大块空白。
- 处理中状态会沿用顶部语音卡片的活动样式，录音结束后的等待、润色和输出阶段更容易被识别。

### 测试

- 使用浏览器自动化检查首页在 1280 和 2048 宽度下的布局，没有发现主要文字重叠或横向溢出。
- 运行 `npm run ai:release-check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test、Rust 依赖审计、clippy 和 Tauri debug build；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- 运行 `npx tauri build`，生成 Windows x64 安装包。

### 审计

- 本次改动只影响首页展示层和发布版本号；不改 ASR、LLM 润色、剪贴板输出、热键、托盘、统计正文记录、日志脱敏或配置结构。

## [0.1.63] - 2026-05-09

### 改进

- 压缩首页顶部语音卡片：移除装饰性功能胶囊，将启动方式改为单行紧凑标签，减少小窗口下的纵向滚动。
- 清理首页旧功能胶囊对应的未使用图标、样式和三语言文案 key，减少后续维护负担。

### 文档

- 同步 README 和 Wiki 草稿，说明首页启动方式布局、配置页头部精简和屏幕 OCR 中文空格规整行为。
- 明确屏幕 OCR 只使用本轮开始录音时的当前窗口文本，不缓存最近 2-3 份截图 OCR 内容，避免挤占豆包 ASR 上下文预算并降低隐私风险。

## [0.1.62] - 2026-05-09

### 改进

- 移除配置页顶部重复的页面标题和“配置状态 / 重新加载”卡片，保留页面正文中的分组标题与操作入口，让热词、API 配置和选项页更紧凑。
- 将首页“启动方式”合并到顶部语音卡片内，统一主快捷键、鼠标中键和右 Alt 的展示尺寸，并修正录音状态圆点对齐。
- 屏幕 OCR 上下文会自动合并中文字符之间的多余空格，减少 `屏 幕 OCR 上 下 文` 这类测试结果对 ASR 和大模型上下文的干扰。

## [0.1.61] - 2026-05-09

### 修复

- 修复安装版点击“测试 OCR”时可能直接报错 `Cannot change thread mode after it is set. (0x80010106)` 的问题。测试入口会在独立工作线程里调用 Windows OCR，避免受 Tauri 命令线程 COM 模式影响；如果遇到已初始化线程，也会沿用既有线程模式，不再把该状态视为失败。

### 测试

- 补充 `RPC_E_CHANGED_MODE` 回归测试。
- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。
- 运行 `npm run ai:release-check`，覆盖本地检查、Rust 依赖审计、clippy 和 Tauri debug build；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- 运行 `npx tauri build`，生成 Windows x64 安装包。

## [0.1.60] - 2026-05-09

### 新增

- 新增屏幕 OCR 上下文，默认在开始录音时截取当前前台窗口并调用 Windows OCR，本轮临时结果会作为上下文附加给豆包 ASR 和可选大模型，帮助识别人名、文件名、代码标识符和界面词。
- 选项页新增屏幕 OCR 开关和测试入口，可立即识别当前窗口并查看耗时、语言和结果预览。

### 隐私与稳定性

- 屏幕 OCR 只截取当前前台窗口，不截取整个屏幕；OCR 正文不写入日志、统计、诊断报告或配置文件。
- OCR 识别异步执行，等待上限默认 700ms；截图、识别、语言包或超时失败都会自动跳过，不阻断录音、ASR、LLM 润色或粘贴主链路。

### 文档

- 同步 README、英文 README、Wiki 草稿和配置模板，说明屏幕 OCR 的默认状态、隐私边界、失败兜底和 `config.toml` 参数。

### 测试

- 为豆包 ASR 上下文拼接、20 条上下文上限、LLM Prompt 拼接、OCR 文本轻量规整和长度限制补充单元测试。
- 使用本机 Windows OCR 对当前窗口进行 smoke 测试，确认 Windows OCR 可用且耗时在默认超时范围内。
- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run ai:release-check`，覆盖本地检查、Rust 依赖审计、clippy 和 Tauri debug build；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- 运行 `npx tauri build`，生成 Windows x64 安装包。

## [0.1.59] - 2026-05-09

### 改进

- 移除设置页“完整设置 / 收起低频设置”切换入口，所有可见设置按页面分组固定展示，避免默认展开后仍出现收起按钮的矛盾交互。
- 进一步精简设置页：Resource ID、ASR/LLM 超时、字幕精细尺寸/位置/自定义色、主热键启用开关、录音时静音系统音量改为 `config.toml` 配置；界面保留更常用的 ASR 语言、LLM API、字幕预设、备用触发和静音自动停止。

## [0.1.58] - 2026-05-09

### 改进

- 整理设置页信息架构，将低频协议、兼容性和底层超时参数移出普通 UI，保留在 `config.toml` 中配置。
- 默认展开完整设置，避免常用开关被高级设置折叠隐藏；同时保留精简设置入口，方便只看基础项。
- 首页改为展示触发方式摘要和编辑入口，避免在首页直接暴露多个开关导致信息分散。
- 将粘贴方式和恢复剪贴板放到选项页的输入结果区域，减少单独兼容性面板带来的割裂感。
- 优化完整设置切换按钮在窄窗口和英文界面下的换行与堆叠表现。

### 文档

- 同步更新 README、英文 README、Wiki 草稿、配置模板和代码规范说明，明确隐藏配置项仍可通过 `config.toml` 调整。

### 测试

- 使用浏览器自动化扫描简体中文、繁体中文和英文，在首页、热词与提示词、API 配置和选项页检查 1100x680、1280x760 下的横向溢出和控件越界。
- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。
- 运行 `npm run ai:release-check`，覆盖本地检查、Rust 依赖审计、clippy 和 Tauri debug build；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- 运行 `npx tauri build`，生成 Windows x64 安装包。

### 审计

- 本次改动只整理设置页展示层和隐藏字段保存体验；不改配置结构、默认值、ASR 请求、LLM 润色触发条件、剪贴板输出、热键默认策略、托盘、统计正文记录或日志脱敏。

## [0.1.57] - 2026-05-07

### 改进

- 优化“预览最终 Prompt”，在预览顶部直接显示场景上下文，并明确最近上下文不进入 LLM Prompt，仅用于豆包 ASR 的 `corpus.context`。
- 移除热词与提示词高级设置中的“清除最近上下文”按钮，减少低频操作入口；最近上下文本地文件和后端清理能力保持不变。

### 文档

- 同步更新 README、英文 README 和 Wiki 草稿，说明 Prompt 预览中的上下文策略，并修正最近上下文清理方式。
- 纳入 `main` 上未发布的 README/Wiki SEO 优化和 2026-05-04 项目审计记录。

### 依赖

- 纳入 Dependabot 前端依赖组合更新。

### 测试

- 运行 `npm run check`，前端类型检查通过。
- 运行 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。
- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run ai:release-check`，覆盖本地检查、Rust 依赖审计、clippy 和 Tauri debug build；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- 运行 `npx tauri build`，生成 Windows x64 安装包。

### 审计

- 本次功能改动只影响前端设置页和 Prompt 预览；不改 ASR、LLM 实际请求策略、剪贴板输出、热键、托盘、统计正文记录、日志脱敏或配置结构。

## [0.1.56] - 2026-05-02

### 修复

- 重新排序首页卡片，将“输入表现”提升到“启动方式”之前，避免完成输入或录音状态下统计卡片被挤到首屏底部。
- 移除首页成功态下“启动方式”的旧压缩布局，让“输入表现”保持完整卡片展示。

### 测试

- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。
- 运行 `npm run ai:release-check`，覆盖本地检查、Rust 依赖审计、clippy 和 Tauri debug build；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- 运行 `npx tauri build`，生成 Windows x64 安装包。
- 使用 Playwright 打开本地首页并截图，确认“输入表现”完整显示在“启动方式”之前。

### 审计

- 本次改动只影响首页布局和发布版本号；不改 ASR、LLM 润色、剪贴板输出、热键、托盘、统计正文记录、日志脱敏或配置结构。

## [0.1.55] - 2026-05-02

### 修复

- 优化首页各状态下的排版稳定性，首页内容区允许纵向滚动，避免完成输入、错误或配置缺失状态下“输入表现”等核心数据被截断。
- 将完成输入后的首页状态标题改为短标题，完整说明保留在描述区，降低简体中文、繁体中文和英文界面下的标题挤压。
- 调整首页启动方式和输入表现卡片的响应式列数与换行策略，减少中等窗口宽度和英文长文案下的横向越界、单列挤压和数据省略。

### 测试

- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run audit:rust`，仅报告当前 Tauri 依赖链中已允许的 RustSec warnings。
- 运行 `npm run check`、`npm run build` 和 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。

### 审计

- 本次改动只影响首页布局、状态标题和发布版本号；不改 ASR 请求、LLM 润色、剪贴板输出、热键、托盘、统计正文记录、日志脱敏或配置结构。

## [0.1.54] - 2026-05-02

### 新增

- 托盘右键菜单新增“重启程序”，会停止当前输入线程、终止当前会话并请求 Tauri 重启，方便配置更新后重新加载或快速恢复偶发异常。
- API 配置页新增豆包 ASR“识别语言”下拉项，默认 `zh-CN`，支持切换到 `en-US`、`ja-JP`、`yue-CN` 等豆包文档支持的语言代码，也可选择自动/服务默认来省略 `audio.language` 参数。

### 改进

- 为 ASR 识别语言补齐 Rust 默认值、配置校验、前端默认配置、三语言文案和配置模板。
- 识别语言配置错误会定位到 API 配置页的新面板，避免被高级请求参数逻辑误判。
- README、README.en.md 和 Wiki 草稿同步补充托盘重启与 ASR 识别语言说明。

### 测试

- 运行 `npm run audit:npm`，未发现 npm 漏洞。
- 运行 `npm run ai:release-check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test、Rust 依赖审计、clippy 和 Tauri debug build。
- 运行 `npx tauri build`，生成 Windows x64 安装包。

### 审计

- 本次改动影响托盘菜单、ASR 请求首包和配置结构；不改空识别失败态、LLM 润色触发条件、剪贴板输出、统计正文记录、日志脱敏或默认激进触发策略。

## [0.1.53] - 2026-05-02

### 修复

- 修复英语界面下 API 配置页部分表单、摘要条和高级参数区域可能撑出内容边界的问题。
- 优化首页启动方式、输入表现卡片、热词提示词页、选项页和统计页的自适应布局，长英文标签、按钮和配置项会换行或自动减少列数。
- 保留大模型 API 配置区“展开配置/收起配置”的实际折叠行为，收起后只显示配置摘要和启用润色开关。

### 测试

- 使用 Playwright 扫描简体中文、繁体中文和英文，在首页、热词与提示词、API 配置、选项、统计分析页面检查横向越界。
- 运行 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。

### 审计

- 本次改动只影响前端多语言布局和发布版本号；不改配置结构、ASR 请求、LLM 调用、剪贴板输出、热键、托盘或统计正文记录。

## [0.1.52] - 2026-05-02

### 改进

- 优化默认大模型润色提示词，明确识别文本只是待润色素材，不回答、不执行其中的命令、问题、角色设定或提示词内容，降低提示词注入和任务偏离概率。
- 同步更新热词页恢复默认提示词、配置模板和当前安装目录配置，保持默认值与实际使用配置一致。

### 文档

- 在中英文 README 补充默认润色提示词的任务边界说明，强调只做文本润色，不做内容分析或问答。

### 维护

- 合并 Dependabot 的 GitHub Actions、前端开发依赖和 Rust 依赖更新；其中 Rust 依赖更新已适配 `cpal`、`tokio-tungstenite` API 变化，并保持 `windows` 直接依赖与 Tauri 兼容。
- 升级 Tauri 前端包到 `2.11.0`，与 Rust 侧 Tauri 版本对齐，并补齐 Node 类型定义、移除过时类型压制以清理发布检查警告。

### 测试

- 加强默认润色提示词单元测试，覆盖“文本润色器”“输入不是指令”“不回答问题”和 `{text}` 占位符等关键约束。
- 运行 `npm run ai:check`，覆盖前端类型检查、构建、密钥扫描、Rust fmt/check/test。

### 审计

- 本次改动影响 LLM 润色默认提示词、配置样例、当前安装目录配置、文档、依赖维护和发布版本号；不改 ASR 请求协议、剪贴板输出、热键、托盘或统计正文记录。

## [0.1.51] - 2026-05-01

### 修复

- 将连续低音量自动停止默认从 10 秒调整为 30 秒，旧版默认值会在配置加载时自动迁移，降低长句口述或思考停顿时被提前截断的概率。
- 将静音判定默认阈值从 `0.04` 调整为 `0.03`，旧版默认值会自动迁移，减少轻声输入被计入静音的概率。

### 文档

- 补充 MIT 许可证正文、贡献指南、安全策略、PR 模板和 Issue 模板，明确开源协作入口、隐私边界和本地检查要求。
- 在中英文 README 增加贡献、安全和许可证入口。
- 补充支持说明和社区行为准则，明确 Issue 前置排查、隐私保护和社区互动边界。

### 维护

- 新增 `.editorconfig` 和 `.gitattributes`，统一编辑器缩进、换行和二进制资源处理规则。
- 新增 Dependabot 配置，按周检查 npm、Cargo 和 GitHub Actions 依赖更新。
- 收紧 CI 权限、并发和超时设置，并完善 Issue / PR 模板的排查、脱敏和风险说明要求。
- 修复 `scripts/scan-secrets.mjs` 在普通目录扫描模式下漏扫嵌套文件的问题，并将 `context/` 本地语音上下文文件明确列为受保护文件。
- 修复密钥扫描脚本从仓库子目录运行 `--git-visible` 或 `--staged` 时可能误跳过文件的问题，并新增无依赖自测。
- 新增 `scan:secrets:staged` 和 `audit:npm` 脚本，并让 CI 覆盖密钥扫描自测和 npm 依赖审计。
- 新增 `audit:rust` 和发布前 Rust 依赖审计入口，并让 CI 安装固定版本 `cargo-audit` 后执行 RustSec 审计。

### 测试

- 补充静音自动停止秒数和音量阈值的旧默认迁移单元测试。

### 审计

- 本次修复只影响本地静音自动停止默认值、配置迁移、当前安装目录配置、文档和发布版本号；不改 ASR 请求、LLM 润色、剪贴板输出、热键、托盘或统计正文记录。

## [0.1.50] - 2026-04-29

### 修复

- 自动热词历史默认上限从 10000 字降为 5000 字，旧版默认值会在配置加载时自动迁移，减少生成候选时的输入规模和失败概率。
- 自动热词候选生成失败后的压缩重试窗口调整为 3000 字，并减少候选数量，避免默认场景下重试仍使用过大的历史文本。

### 测试

- 补充默认值迁移和压缩重试单元测试，覆盖旧版默认 10000 字迁移到 5000 字、自定义上限保留，以及大历史文本重试窗口。

### 审计

- 本次改动影响自动热词候选配置默认值、热词生成失败重试策略、UI 默认状态、配置样例和文档；不改 ASR、LLM 润色、剪贴板输出、热键、托盘和统计正文记录。

## [0.1.49] - 2026-04-29

### 修复

- 自动热词候选生成请求不再人为限制 `max_tokens`，避免历史文本较长时模型返回的 JSON 被截断。
- 自动热词候选生成遇到模型截断、空响应或不完整 JSON 时，会给出更明确的用户提示，不再只显示通用“操作失败”。

### 审计

- 通过 npm `overrides` 将传递依赖 `cookie` 固定到 `0.7.2`，清理发布前依赖审计中的低危告警。
- 本次改动只影响自动热词候选生成、错误提示、依赖锁定和发布版本号；不改 ASR、剪贴板输出、热键、托盘、统计正文记录或配置结构。

## [0.1.48] - 2026-04-29

### 修复

- 修复切换为英文或繁体中文后，Windows 托盘右键菜单仍显示简体中文的问题。
- 托盘 tooltip 现在会跟随语言切换，并在录音/处理阶段保持正确状态。
- 设置健康检查、窗口按钮提示、启动兜底页、更新成功提示、连接测试成功提示和自动热词清理提示不再直接透出固定中文。

### 测试

- 补充托盘菜单和 tooltip 的语言选择单元测试，覆盖英文、繁体中文和未知语言回退。
- 运行前端类型检查，确认新增三语言文案和调用链无类型错误。

### 审计

- 本次改动只影响用户界面语言展示和发布版本号；不改 ASR、LLM 请求、剪贴板输出、热键触发、统计正文记录或配置结构。

## [0.1.47] - 2026-04-29

### 变更

- 优化本地连续低音量自动停止判断：短促高音量噪声不会立即重置静音计时，降低键盘声、碰麦等瞬时噪声拖长空录音的概率。

### 测试

- 补充静音自动停止去抖单元测试，覆盖短促噪声忽略和连续有效声音跨多个音频块重置计时。

### 审计

- 本次优化只影响本地静音自动停止的重置条件；不改 ASR、LLM、剪贴板、热键、托盘、统计正文记录或配置结构。
- 依赖审计发现 SvelteKit 传递依赖 `cookie<0.7.0` 低危告警；SvelteKit 最新版仍声明该依赖范围，`npm audit fix --force` 会给出不合理降级方案，本次不强行引入覆盖依赖。

## [0.1.46] - 2026-04-29

### 修复

- 修复 Windows 剪贴板写入已成功但读回校验偶发返回 `0x8007058A` 时，自动粘贴 keyboard event 未继续发送的问题；读回失败现在降级为警告并继续输出。

### 变更

- 将自动粘贴后的剪贴板恢复默认等待调整为 800ms，安全下限调整为 500ms，减少粘贴完成后的等待时间。

### 测试

- 补充写入后读回失败不应阻断输出的单元测试。

### 审计

- 本次修复只影响剪贴板输出阶段；不改 ASR、LLM、热键、托盘、统计正文记录或配置结构。

## [0.1.45] - 2026-04-28

### 修复

- 自动粘贴的 keyboard event 发送完成后立即释放本轮会话，避免粘贴成功后立刻按快捷键启动下一轮语音输入时被 `Pasting` 状态短暂拦截。

## [0.1.44] - 2026-04-28

### 修复

- 将普通自动粘贴后的悬浮字幕隐藏时机前移到粘贴快捷键发送完成后，不再等待剪贴板恢复流程结束。

### 测试

- 补充空文本输出不会触发“输出已发送”回调的单元测试，避免空识别误进入成功态。

### 审计

- 本次修复仅调整剪贴板输出后的悬浮字幕隐藏时机；不改剪贴板恢复策略、ASR、LLM、热键、托盘、配置结构或统计内容。

## [0.1.43] - 2026-04-28

### 变更

- 普通成功输出完成后立即隐藏悬浮字幕，不再等待最近上下文、自动热词和统计收尾完成后才消失。
- 剪贴板输出成功但存在需要用户注意的告警时，立即把告警显示到悬浮字幕，减少粘贴完成后的反馈延迟。

### 测试

- 补充悬浮字幕告警停留策略单元测试，确认普通成功和静默剪贴板提示不会延长字幕显示。

### 审计

- 本次精修仅调整剪贴板输出完成后的悬浮字幕收尾时机；不改 ASR、LLM、配置结构、热键、托盘、统计内容或隐私边界。

## [0.1.42] - 2026-04-28

### 新增

- 托盘右键菜单新增“检查更新”，会唤起主窗口并复用现有手动更新检查流程。

### 变更

- 托盘输入中状态点改为更克制的绿色指示，避免红色误解为错误告警。
- 左下角状态卡片将状态点移到标题右侧，标题与说明文案左边缘保持一致。
- 统计分析按日表格的语音时长改为分钟展示，并优化数值列宽、右对齐和等宽数字排版。
- 英文首页缩短部分长标签，允许 hero 特性标签自然换行，降低完成输入提示卡在英文下的占用高度。

### 审计

- 本次精修仅影响 UI 展示、托盘菜单触发更新检查和文档说明；不改 ASR、LLM、剪贴板、热键默认值、统计原始数据或配置结构。

## [0.1.41] - 2026-04-28

### 修复

- 修复 Windows 透明悬浮字幕窗长时间运行后偶发处于“窗口可见但内容不绘制”的状态；现在每次显示字幕窗后会重新确认置顶并触发一次系统重绘。

### 审计

- 复核本次修复仅影响悬浮字幕显示层，不改 ASR、LLM、剪贴板、热键、托盘、统计或配置结构。

## [0.1.40] - 2026-04-27

### 修复

- 修复悬浮字幕显示时可能抢占目标输入窗口焦点的问题。
- 修复自动输出失败时仍提前写入最近上下文、自动热词历史和成功统计的问题；现在只有剪贴板写入、自动粘贴和剪贴板恢复流程返回成功后，才记录这些成功副作用。

### 审计

- 日志写入前新增本机用户目录脱敏，避免日志默认暴露 Windows 用户名路径。
- 复核主链路：空识别仍进入失败态；LLM 润色状态只在启用、达到 `min_chars` 且 Base URL、API Key、模型名完整时出现；悬浮字幕不显示“正在粘贴”或“已粘贴”。

## [0.1.39] - 2026-04-27

### 变更

- 提高本地低音量自动停止阈值到 `0.04`，默认把过低音量识别为无效静音。
- 本地静音超时改为复用手动停止链路，按配置保留尾音后再等待最终 ASR 结果。
- 移除启动、托盘恢复和重复启动唤起后的额外主窗口居中，避免窗口位置被意外改变。
- 同步中英文文档和配置示例中的低音量兜底说明。

## [0.1.38] - 2026-04-27

### 新增

- 新增本地连续静音兜底：默认连续静音 10 秒会自动结束本次录音，避免豆包服务端判停未触发时一直录到最长 300 秒。

### 变更

- 豆包 ASR `end_window_size` 默认对齐为 800ms；已有 `config.toml` 中的自定义值继续保留。
- 首页在最近输入与启动方式并排展示时进一步压缩启动方式文字、按钮和勾选图标，减少文字折叠。
- 设置页发现新版本时，更新按钮直接显示“立即更新”，并为静音自动停止秒数补充 0 到 300 秒输入范围。

### 审计

- 复核主流程：空识别仍进入失败态，不会触发润色、粘贴或成功统计。
- 复核隐私边界：最近一次输入仅保留在前端窗口内存，日志、统计和诊断报告不记录识别正文。

## [0.1.37] - 2026-04-27

### 变更

- 输入、等待结果、润色和粘贴处理期间，系统托盘图标切换为带状态点的输入中样式，结束后恢复普通图标。

## [0.1.36] - 2026-04-27

### 变更

- 发现新版本的提示增加“立即更新”按钮，可直接启动下载并自动安装流程。
- 非编辑区域屏蔽 WebView 默认右键菜单，避免首页弹出 Refresh、More Tools 等浏览器菜单。

## [0.1.35] - 2026-04-27

### 变更

- 首页移除语音输入卡片标题，并统一“启动方式”和“输入表现”的标题样式，减少图标和标题层级噪音。
- 完成输入卡新增“复制文本”按钮，方便用户直接复制最近一次识别文本。

### 技术实现

- 新增专用的最近输入文本复制命令，复用现有剪贴板写入校验逻辑，不触发粘贴、不记录识别正文。
- 收紧首页复制状态处理，避免复制过程中开始新录音时误更新旧结果状态。

## [0.1.34] - 2026-04-27

### 变更

- 首页改为“语音输入全宽、最近输入与启动方式并排、输入表现全宽”的结构，恢复最近一次输入内容入口，同时避免压缩统计表现区域。
- 首页普通状态下隐藏内容区滚动条，仅在配置缺失或错误详情需要展示时允许滚动，减少日常使用中的额外滚动干扰。

### 修复

- 修复 Windows 启动时同步开机自启动注册表可能闪现黑色控制台窗口的问题。

### 审计

- 复核最近一次输入内容仅保留在前端窗口内存，不写入统计文件或日志。
- 补齐 v0.1.33 更新日志记录，避免发布历史断档。

## [0.1.33] - 2026-04-27

### 变更

- 重构热词、API 配置和选项页的信息层级：默认展示必要和高频设置，高级区域承载低频排障项，底层实现参数保留 `config.toml` 支持。
- 大模型 API 配置默认保留核心字段和测试连接，将超时、thinking 等低频参数移入高级设置。
- 热词页保留常用热词和润色提示词入口，将最近上下文和自动热词候选收进高级设置。
- 选项页默认聚焦快捷键、粘贴方式、麦克风、字幕外观、开机启动和关闭行为。

### 修复

- 修复自动更新静默安装后不会自动打开新版本的问题，NSIS 安装器改用 `/S /R` 参数。
- 修复隐藏的 config-only 高级字段校验错误阻断可见设置保存的问题，未修改的隐藏字段会保留原值。

### 测试

- 补充更新安装参数测试，确认静默安装后会请求重新打开应用。
- 补充隐藏高级配置字段保存兼容性测试。

## [0.1.32] - 2026-04-27

### 变更

- 大模型 API 配置页将“思考模式”开关放到大模型配置区，并补充三语言说明，提示语音润色通常不需要开启。
- 首页移除完成输入后的正文回执卡，减少首页占高，并避免在主窗口继续保留识别正文。
- 首页隐藏外层标题行，将启动方式和最近使用改为宽窗口双栏展示，减少普通状态下的额外滚动条。
- 首页状态说明和功能标签增加溢出保护，降低文案变长导致布局被撑高的概率。

### 修复

- 修复部分思考模型在连通性测试中只返回 reasoning 内容时被误判为空响应的问题。

### 测试

- 补充 LLM 连通性测试响应解析单元测试，覆盖 reasoning-only 响应、空响应和测试 token 上限。

## [0.1.31] - 2026-04-26

### 变更

- 应用内更新下载 NSIS 安装包后改为静默启动安装，并自动退出当前版本释放文件，减少用户在更新流程中反复点击“下一步”或卸载确认。
- 保存设置和启动阶段将开机自启动同步、全局热键重启等慢副作用移到后台执行，减少切换设置项或启动时的界面卡顿。
- 选项页默认展示软件更新区块和启动时自动检查更新开关，仅将 GitHub Release Repo 等排障参数保留在高级设置中。
- 自动粘贴前再次确认剪贴板仍是本次识别文本，若被改写会重新写入；同时提高恢复原剪贴板前的安全等待，减少慢应用粘贴到旧剪贴板内容的问题。
- 将剪贴板恢复延迟默认值调整为 1800ms，并在自动粘贴时对更低配置使用安全下限。
- 新增“自动移除句末句号”选项，默认保持开启；关闭后保留 ASR 或大模型输出的句末标点。

### 测试

- 补充配置保存副作用判断测试，确认无关设置不会触发热键重启或开机启动同步。
- 补充更新安装参数测试，确认应用内更新使用 NSIS 静默安装参数。
- 补充剪贴板输出时序测试，覆盖粘贴前文本匹配判断和恢复延迟安全下限。
- 补充最终文本句末标点开关测试，覆盖开启移除和关闭保留两种行为。

## [0.1.30] - 2026-04-26

### 变更

- 将前端 session 阶段判断、quiet warning 判断和前端诊断日志工具抽为独立纯函数模块，减少 `VoxTypeController` 的策略代码。
- 移除最近输入结果中未展示的错误分支状态，错误继续由首页错误卡片统一承载。
- 将配置校验逻辑从 `config.rs` 拆入 `config_validation.rs`，让配置模型、IO 和字段校验职责更清晰。
- 新增轻量错误上下文工具，先收敛配置与最近上下文路径中的重复错误字符串拼接。

### 测试

- 补充 `recording` 阶段 `toggle` 仍可进入停止流程的回归测试。

## [0.1.29] - 2026-04-26

### 新增

- 首页新增最近一次输入完成提示，成功后保留 `Ctrl + V` 兜底说明。
- 首页错误提示新增可执行操作按钮，支持重试、跳转 API 配置、打开选项、打开日志和复制诊断报告。

### 修复

- 录音 `starting` / `stopping` 阶段加入重复触发保护，减少连续点击或连续热键造成的状态困惑。
- 主界面未知 Tauri 调用错误改为用户友好提示，原始错误仅保留在日志中。
- 移除页面内容区重复展示的 `VoxType` 标题，修复首页、选项和统计页的标题排版噪音。
- 修复 Windows lib 单元测试缺少 common controls v6 manifest 时无法启动的问题。

### 测试

- 补充 `starting` / `stopping` 阶段 `toggle` 防重复触发单元测试。

## [0.1.28] - 2026-04-26

### 修复

- 收紧诊断报告本地路径脱敏边界，避免相似用户目录前缀被误判为用户目录。

### 变更

- 将诊断路径脱敏逻辑抽为可单独测试的纯函数，降低隐私相关代码的维护成本。

### 测试

- 补充路径脱敏单元测试，覆盖用户目录精确匹配、子路径匹配和相似前缀不应脱敏的场景。

## [0.1.27] - 2026-04-26

### 修复

- 修复手动检查更新失败时不再显示错误提示的回归，保持拆分前的用户反馈行为。

### 变更

- 整理主应用控制器 import 结构，减少上轮拆分后留下的阅读噪音。
- 复核主应用重构后的文件规模，`VoxTypeController` 继续保持在 50 KiB 以下。

## [0.1.26] - 2026-04-26

### 变更

- 将主应用状态与命令处理迁移到 `VoxTypeController`，让 `VoxTypeApp` 保持为轻量渲染组件。
- 拆出悬浮字幕、自动热词、更新、诊断和窗口控制器，继续降低主应用组件复杂度。
- 将麦克风音量条计算抽为前端工具函数，减少界面控制器中的展示细节。

### 测试

- 补充健康检查单元测试，确认隐私、额外触发方式和系统静音等软选项不会阻断 ready 状态。
- 补充触发方式摘要单元测试，覆盖多触发方式展示和未启用状态。

## [0.1.25] - 2026-04-26

### 变更

- 将 `+page.svelte` 降为轻量路由入口，主应用拆为 `VoxTypeApp`、`AppShell`、`AppContent` 和全局样式组件。
- 拆出首页、热词、API 配置、选项和统计页面级组件，降低路由入口与主模板复杂度。
- 将设置表单、首页卡片和悬浮/弹窗相关样式迁移到对应组件内，减少路由文件样式负担。

### 测试

- 补充健康检查状态单元测试，覆盖缺少 ASR 密钥、无麦克风、无触发方式和 ready 状态。

## [0.1.24] - 2026-04-26

### 新增

- 新增页面级高级设置开关，热词、API 配置和选项页可分别展开高级内容。
- 新增发布前本地检查入口 `npm run ai:release-check`，统一执行本地检查、clippy 和 Tauri debug 构建。

### 变更

- 热词页默认只展示手动热词，场景上下文、大模型 Prompt 和自动热词候选移入高级设置。
- API 配置页突出豆包 ASR 必填认证，并将大模型 API 标记为可选配置。
- 选项页默认只展示日常设置，剪贴板、录音、更新和诊断参数移入高级设置。
- 首页健康检查支持 warning 等级，非阻断注意事项改为折叠展示。
- CHANGELOG 改为简体中文维护。

### 修复

- 修正 API 配置页与选项页共用高级设置开关导致跨页互相影响的问题。

## [0.1.23] - 2026-04-26

### 新增

- 新增独立的“已接受自动热词”列表，让生成热词与用户维护的自定义热词在界面上保持区分。

### 变更

- ASR 请求、LLM 润色提示词、提示词预览和自动热词去重过滤改为使用合并后的有效热词列表，但配置中仍保留分开的存储列表。
- 优化悬浮字幕布局，音量指示不再遮挡多行字幕文本，浏览器换行也不会额外制造视觉错行。

## [0.1.22] - 2026-04-26

### 新增

- 新增 Windows GitHub Actions CI 流程，覆盖前端检查、密钥扫描、Rust 格式检查、clippy 和测试。

### 变更

- 将共享前端应用类型从 `src/routes/+page.svelte` 移到 `src/lib/types/app.ts`，作为页面和组件拆分的第一步低风险整理。
- 为 ASR 空结果处理、处理中阶段会话切换、非法音频采样率校验补充聚焦的 Rust 回归测试。
- 为高风险的配置、会话和剪贴板输出边界补充文档注释。
- 新增结构化 ASR warning code，让低优先级剪贴板恢复提示可以不依赖本地化文本匹配来隐藏。

### 修复

- 默认隐藏较大的“部分剪贴板格式未备份”提示，不再显示到悬浮字幕和主窗口 toast，但仍保留日志中的诊断 warning。

## [0.1.21] - 2026-04-26

### 变更

- 自动热词状态不再向前端暴露本地历史文件路径。
- 自动热词生成失败时，不再把服务原始响应正文透传到错误处理路径。
- 热词历史测试现在会清理临时目录。
