# VoxType release audit - 2026-05-09

## Release

- Version: `0.1.73`
- Tag: `v0.1.73`
- Installer: `VoxType_0.1.73_x64-setup.exe`
- SHA256: `71BCB1921E7DD138F544E6A8570F5C8BC861F2DBF305DAD5B47C92FD6DF5E4F5`

## Changes

- Refined the titlebar and startup toast app icon presentation so the SVG icon renders larger and cleaner.
- Regenerated Windows/Tauri icon assets from the selected SVG with transparent exterior corners for tray and taskbar use.
- Added explicit NSIS installer and uninstaller icon configuration so installer executables use the app icon.

## Verification

- `npm run ai:release-check` passed.
- `npx tauri build` produced the Windows x64 NSIS installer.
- Browser smoke check covered the main UI at 1280px and 600px widths.
- Extracted the associated icons from both `voxtype-desktop.exe` and the NSIS installer; both showed the new blue-white icon.
- Checked icon alpha: generated `128x128.png` and `icon.png` corners are transparent, and the 32px tray icon keeps only anti-aliased near-transparent corner pixels.

## Scope audit

- This release only changes icon assets, frontend icon presentation, Tauri bundle icon configuration, and release metadata.
- It does not change configuration structure, ASR requests, LLM post-edit trigger conditions, clipboard output, hotkey defaults, usage statistics text policy, or log redaction.
