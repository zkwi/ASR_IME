# VoxType release audit - 2026-05-09

本记录用于保存 `0.1.72` 发布前审计的非敏感结论。不要在审计记录中写入真实密钥、识别正文、屏幕 OCR 正文、热词、prompt、最近上下文、日志正文或本机用户名路径。

## 范围

- 应用品牌图标资源替换。
- 主界面标题栏、启动提示、favicon、托盘、快捷方式、窗口和安装包图标一致性。
- 发布版本号和变更记录同步。

## 结论

- 新图标已统一为白蓝色麦克风按键图标，并生成 Tauri 所需的 PNG、ICO、ICNS、Windows Square/Store 和托盘 RGBA 资源。
- 前端标题栏和启动提示已改为使用同一 `app-icon.png`，favicon 已同步替换。
- 本次不改配置结构、ASR 请求协议、LLM 润色触发条件、剪贴板输出策略、热键默认值、统计正文记录或日志脱敏策略。
- `npm run ai:release-check` 通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- `npm run audit:npm` 未发现 npm 漏洞。
- `npx tauri build` 已生成 Windows x64 NSIS 安装包：`VoxType_0.1.72_x64-setup.exe`。
- 安装包 SHA256：`CB539F18F225C9E2A34160BCE12AEA44BFDEC263101DE48F7DDD3C321485A69B`。

## 已运行检查

```powershell
npm run ai:check
npm run ai:release-check
npm run audit:npm
npx tauri build
```

## 手工回归建议

- 安装后检查桌面快捷方式、开始菜单、任务栏、窗口标题、托盘常驻图标和托盘右键菜单显示的图标是否均为新版白蓝图标。
- 启动应用后检查标题栏左侧图标和启动提示图标是否加载正常。
- 录音中检查托盘图标的绿色活动状态点是否仍清晰可见。
