# VoxType release audit - 2026-05-09

本记录用于保存 `0.1.69` 发布前审计的非敏感结论。不要在审计记录中写入真实密钥、识别正文、屏幕 OCR 正文、热词、prompt、最近上下文、日志正文或本机用户名路径。

## 范围

- 治理脚本中的三语言 i18n key parity 检查。
- 治理脚本自测中的真实 i18n 写法、缺失 key 和多余 key 覆盖。
- 版本号、CHANGELOG、发布检查和安装包产物。

## 结论

- i18n key parity 检查现在会递归识别嵌套的 `as const` / `satisfies` 文案对象，更贴近真实三语言文案文件写法。
- 治理自测覆盖缺失 key 和多余 key 两类文案漂移问题，降低后续维护时漏同步的风险。
- 本次不改应用运行时代码、配置结构、ASR 请求协议、LLM 润色触发条件、剪贴板输出策略、热键默认值、统计正文记录或日志脱敏策略。
- `npm run ai:release-check` 通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的 19 条上游 warning。
- `npm run audit:npm` 未发现 npm 漏洞。
- `npx tauri build` 已生成 Windows x64 NSIS 安装包：`VoxType_0.1.69_x64-setup.exe`。
- 安装包 SHA256：`B8732C99C573EC42A5E574AAD7E7F7AFD9BBAD8BFC5AE267F192F2A5F7ACEE3B`。

## 已运行检查

```powershell
npm run ai:release-check
npm run audit:npm
npx tauri build
```

## 手工回归建议

- 本次没有改应用运行时代码，优先确认 `npm run check:governance` 对真实仓库通过。
- 后续新增任意三语言文案时，可临时删除或增加一个语言 key，确认治理脚本能阻断文案漂移。
