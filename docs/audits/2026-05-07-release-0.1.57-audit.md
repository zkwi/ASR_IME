# VoxType release audit - 2026-05-07

本记录用于保存 `0.1.57` 发布前审计的非敏感结论。不要在审计记录中写入真实密钥、识别正文、热词、prompt、最近上下文、日志正文或本机用户名路径。

## 范围

- 本轮 UI 与 Prompt 预览改动。
- `v0.1.56..HEAD` 中尚未发布的文档审计、SEO 文档优化和前端依赖更新。
- 版本号、CHANGELOG、README、英文 README 和 Wiki 草稿一致性。
- 发布前本地检查、密钥扫描、依赖审计和构建产物。

## 结论

- “清除最近上下文”按钮已从前端设置页移除，后端清理命令保留，不影响最近上下文开关、轮数设置或 ASR 上下文构造。
- “预览最终 Prompt”只改变本地预览文本：场景上下文继续进入 LLM Prompt；最近上下文仍不进入 LLM Prompt，仅用于豆包 ASR `corpus.context`。
- 本次改动不改变 ASR 请求、LLM 实际请求策略、剪贴板输出、热键、托盘、统计正文记录、日志脱敏或配置结构。
- README、README.en.md 和 docs/wiki/Setup-Guide.md 已同步上下文策略和最近上下文清理说明。
- 未新增依赖，未发现被 Git 跟踪的本地配置、日志、统计、最近上下文或自动热词历史文件。
- npm audit 为 0 vulnerabilities。
- `npm run ai:release-check` 通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的 19 条上游 warning。
- `npx tauri build` 已生成 Windows x64 NSIS 安装包：`VoxType_0.1.57_x64-setup.exe`。
- 安装包 SHA256：`FADB2C298272FDDAD173E8C3CC4E221BF135AEA9056F5A89FFFD6093A0C1281D`。

## 已运行检查

```powershell
git fetch origin --tags --prune
git diff --check
npm run check
npm run ai:check
npm run audit:npm
npm run ai:release-check
npx tauri build
```

## 手工回归建议

- 打开热词与提示词高级设置，确认“清除最近上下文”按钮不再显示。
- 点击“预览最终 Prompt”，确认顶部摘要能看到场景上下文内容或空状态，并看到最近上下文不进入 LLM Prompt 的说明。
- 开启最近上下文后完成一次语音输入，确认 ASR、LLM 润色、粘贴和统计主链路仍正常。
