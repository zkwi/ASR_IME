# 2026-05-30 LLM 文档同步审计

本记录用于追踪一次围绕大模型评估结论修正后的项目文档同步审计。

## 审计范围

- README / README.en 中的大模型配置与模型选择说明。
- `docs/README.md` 文档索引。
- Wiki 草稿：Home、Setup Guide、Feature Guide、Troubleshooting 及英文版本。
- 已有模型评估文档：`docs/audits/2026-05-28-llm-polishing-model-test.md`。

## 审计结论

- 已删除或修正“技术文本较多优先 `deepseek-v4-pro`”这类过期推荐。
- 已明确 2026-05-30 复测结论：当前简化 prompt 下，多个模型都会把代码路径、文件名或英文标识符改成看似合理但错误的形式，不能只靠切换模型解决。
- 已统一说明性能排查优先级：更换 Base URL 或模型后重新运行 API 配置页测试，让 VoxType 保存最快的 `thinking_strategy`；不要把增加超时当作加速手段。
- 已统一说明准确性建议：技术路径、文件名、日志字段和英文标识符应优先依赖屏幕 OCR、热词或人工确认。

## 未改变内容

- 不改默认 prompt。
- 不改 `thinking_strategy` 默认值或请求适配代码。
- 不改 ASR、LLM 润色触发条件、剪贴板输出、热键默认值、托盘行为、日志脱敏或统计正文策略。
- 不包含真实 API Key、真实识别正文、本地最近上下文、热词、prompt、日志或统计正文。

## 建议回归

- 打开 README 和 Wiki 配置指南，确认模型选择说明都指向修正后的评估文档。
- 打开 Troubleshooting，确认“大模型润色太慢”和“技术路径或文件名被改错”都有可执行排查建议。
- 在 API 配置页更换模型后重新测试一次，确认 UI 仍会保存最快的思考适配策略。
