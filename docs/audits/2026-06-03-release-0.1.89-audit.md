# VoxType release audit - 2026-06-03

本记录用于保存 `0.1.89` 发布前审计的非敏感结论。不要在审计记录中写入真实密钥、识别正文、屏幕 OCR 正文、热词、prompt、最近上下文、日志正文或本机用户名路径。

## Release

- Version: `0.1.89`
- Tag: `v0.1.89`
- Installer: `VoxType_0.1.89_x64-setup.exe`
- SHA256: `DD2CF720D1B036B21D1D048D15F0DB9A3FA1B34CC3F37B539F4F79BB8F8F267F`
- Size: `5682135` bytes

## Changes

- 停止录音后的尾音保留默认值从 500ms 调整为 800ms，减少松开热键或停止录音时末尾字被截断的概率。
- Rust 默认配置、前端兜底默认值、配置模板、README 和 Wiki 草稿中的录音配置示例已同步。
- 默认值测试补充 `stop_grace_ms = 800` 断言，降低后续误回退概率。

## Verification

- `npm run ai:check` passed before release metadata changes.
- `npm run ai:release-check` passed, including frontend check/build, stats tests, secret scans, governance checks, Rust fmt/check/tests, Rust audit, clippy, and Tauri debug build.
- `npm audit --registry=https://registry.npmjs.org` passed with 0 vulnerabilities.
- Rust audit reported only existing allowed dependency warnings from the Tauri dependency tree.
- `npx tauri build` produced the Windows x64 NSIS installer.

## Scope audit

- This release changes audio default configuration, config example, frontend fallback defaults, README files, Wiki drafts, version metadata, changelog, and release documentation.
- It does not change ASR request protocol, empty-recognition failure behavior, LLM post-edit trigger conditions, clipboard output, hotkey defaults, tray behavior, usage statistics text policy, or log redaction.
