# VoxType release audit - 2026-05-09

本记录用于保存 `0.1.71` 发布前审计的非敏感结论。不要在审计记录中写入真实密钥、识别正文、屏幕 OCR 正文、热词、prompt、最近上下文、日志正文或本机用户名路径。

## 范围

- 英文界面文案、导航术语和普通用户可理解性。
- 首页、热词与提示词、API 配置、选项、隐私与本地数据和统计分析页的样式一致性。
- 统计分析页标题对齐、公共表单样式、面板间距和按钮/checkbox/input 基础样式。

## 结论

- 英文文案已缩短并统一术语，降低英文状态下文本被遮挡的风险。
- 设置类页面已集中使用公共样式，减少同类表单、按钮、checkbox 和说明文字的重复定义。
- Analytics 顶部标题已与统计卡片左边缘对齐，不再居中显示。
- 600px 和 2048px 宽度下巡检主要页面，未发现主页面横向溢出。
- 本次不改配置结构、ASR 请求协议、LLM 润色触发条件、剪贴板输出策略、热键默认值、统计正文记录或日志脱敏策略。
- `npm run ai:release-check` 通过；Rust 审计仅报告当前 Tauri 依赖链中已允许的 warnings。
- `npm run audit:npm` 未发现 npm 漏洞。
- `npx tauri build` 已生成 Windows x64 NSIS 安装包：`VoxType_0.1.71_x64-setup.exe`。
- 安装包 SHA256：`46B8DAFA7CE719E19E1024864AC25929F5AEAFC40BE14AA841A18932CB40BFBD`。

## 已运行检查

```powershell
npm run ai:check
npm run ai:release-check
npm run audit:npm
npx tauri build
```

## 手工回归建议

- 英文模式：Home、Terms、Services、Options、Privacy、Analytics 页面是否还有文字被截断。
- 统计分析：Analytics 标题在宽窗口和窄窗口下是否都与统计卡片左侧对齐。
- 设置类页面：输入框、按钮、checkbox、分组标题和面板间距是否一致。
